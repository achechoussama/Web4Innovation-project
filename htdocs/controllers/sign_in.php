<?php

  include_once("./views/home/sign_in.php");
  //include once is better in performance because (file exists in the cache) but a problem if we change the file and the cache havn t change
  //include == require
 ?>